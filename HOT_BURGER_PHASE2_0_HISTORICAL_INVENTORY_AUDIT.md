# HOT BURGER — Phase 2.0: Historical Inventory Integrity Audit

**Phase Type:** READ ONLY — No Code Changes
**Date:** 2026-08-19
**Author:** Manus AI
**Repository:** `thikryat57-oss/hot-burger-last` (branch `main`, baseline commit `ac65dba`)

---

# 1. Executive Summary

هذا التقرير هو تدقيق READ-ONLY للحالة الفعلية لنظام المخزون التاريخي في تطبيق HOT BURGER، مع التركيز على سؤال واحد جوهري:

> **هل يستطيع النظام إعادة المخزون الصحيح الذي تم خصمه وقت إنشاء الفاتورة إذا تغيّرت وصفة المنتج بعد البيع؟**

**الإجابة من الكود: لا.** عند تغيّر الوصفة بعد البيع، فإن عمليات الإرجاع (`returnInvoice`) والإلغاء (`voidInvoice`) تعيد بناء كمية الاسترجاع من الوصفة **الحالية** (`product_ingredients` كما هي اليوم) وليس من الوصفة التي كانت سارية وقت البيع. النتيجة المباشرة هي إعادة كميات خاطئة للمخزون — أكثر من المخصوم إذا نمت الوصفة، وأقل منه إذا تقلّصت — مع سجل تدقيق يوثّق الرقم الخاطئ وكأنه صحيح.

| السؤال الأساسي | الجواب |
|---|---|
| هل يتم خصم المخزون من الوصفة الحالية وقت البيع؟ | نعم (سلوك صحيح للبيع) |
| هل يُخزَّن snapshot للوصفة على الفاتورة؟ | **لا — لا يوجد أي حقل أو جدول للوصفة التاريخية** |
| هل الإرجاع/الإلغاء يستعيد حسب الوصفة وقت البيع؟ | **لا — يستعيد حسب الوصفة الحالية** |
| هل تغيّر الوصفة بعد البيع يفسد الاسترجاع؟ | **نعم — خطأ مؤكد من الكود (انظر قسم السيناريو الإلزامي §2)** |
| هل COGS المالي محفوظ (frozen snapshot)؟ | نعم — `cost_snapshot` محفوظ على كل سطر فاتورة (تم التحقق في Phase 1.1) |
| هل المخزون المالي آمن تاريخيًا؟ | **نعم (البيانات المالية سليمة) / المخزون الفيزيائي: لا (بعد تغيير الوصفة)** |

النقطة الحاسمة في هذا التدقيق هي التمييز بين أمنين منفصلين:

1. **الأمن المالي (COGS/الأرباح):** سليم. `invoice_items.cost_snapshot` + `unit_profit` + `total_profit` تُجمَّد وقت البيع، ولا تلمسها `returnInvoice` ولا `voidInvoice` إطلاقًا. طبقة التقارير الموحدة (Phase 1/1.1) تعتمد على هذا snapshot.
2. **الأمن الفيزيائي (كميات المخزون):** غير سليم بعد تغيير الوصفة. لا يوجد أي مرجع تاريخي يربط الفاتورة بالكميات الفعلية المستخدمة وقت البيع، والإرجاع يُعاد حسابه من وصفة اليوم.

---

# 2. Sale Flow Trace — تتبع مسار البيع الكامل

المسار الفعلي المُثبت من الكود (`app_provider.dart:535-693`) خطوة بخطوة:

| # | الخطوة | الملف والدالة | الأسطر | السلوك الفعلي | الجدول | حدود المعاملة |
|---|---|---|---|---|---|---|
| 1 | التحقق من الوردية والسلة والقيم | `createInvoice()` — `AppProvider` | 535-587 | يتحقق من وردية مفتوحة، يعيد حساب `subtotal` من السلة ويقارنه بما مرّره الاستدعاء (tolerance 0.01)، يتحقق من صلاحيات الخصم والإجمالي وطريقة الدفع والباقي. **لا يثق بإجماليات الواجهة** | — | قبل المعاملة |
| 2 | حساب المكونات المطلوبة | `createInvoice()` | 591-599 | يستدعي `DatabaseHelper.getProductIngredients(productId)` — **الوصفة الحالية** — ويجمع الكميات حسب `ingredient_id`: `required[id] += perUnit * qty` لكل سطر. المنتجات المكررة والمنتجات المختلفة التي تشترك في نفس المكوّن تُجمَع في رقم واحد | `product_ingredients` (قراءة) | قبل المعاملة |
| 3 | فحص توفر المخزون | `createInvoice()` | 600-605 | يتحقق أن `current inventory quantity >= required` لكل مكوّن؛ يرمي استثناء عند النقص | `inventory` (قراءة) | قبل المعاملة |
| 4 | التقاط تكلفة المنتج (COGS snapshot) | `calculateProductCost(productId)` | 606-609 | يُحسب مرة واحدة لكل منتج عند البيع | `products.cost` (قراءة) | قبل المعاملة |
| 5 | بدء المعاملة | `_db!.transaction<int>` | 611 | SQLite transaction واحدة تغلّف كل شيء حتى نهاية السطر 681 | `invoices` | BEGIN |
| 6 | إنشاء الفاتورة | `txn.insert('invoices')` | 612-625 | `status` = القيمة الممررة (افتراضي 'completed')، `kitchen_status='new'`، كافة المبالغ المالية | `invoices` | داخل المعاملة |
| 7 | إنشاء أسطر الفاتورة | `txn.insert('invoice_items')` × n | 626-635 | يحفظ لكل سطر: `cost_snapshot` (تكلفة لحظة البيع)، `unit_profit = price - cost_snapshot`، `total_profit` — كلها **مجمّدة** | `invoice_items` | داخل المعاملة |
| 8 | خصم المخزون | حلقة على `requiredIngredients` | 636-651 | لكل مكوّن: `before = current quantity`، `after = before - delta`، حارس يمنع السالب، `UPDATE inventory` ثم `INSERT inventory_audit_log` بـ `action_type='sale'` و`quantity_change = -delta` و`cost_price_at_action` و`reference_type='invoice'` + `reference_id` | `inventory`، `inventory_audit_log` | داخل المعاملة |
| 9 | سجل تدقيق الفاتورة | `txn.insert('invoice_audit_log')` | 652-655 | `action_type='created'` + المستخدم المسؤول | `invoice_audit_log` | داخل المعاملة |
| 10 | نقاط الولاء | تحديث `customers` + سجل النقاط | 656-680 | نقطة لكل 10 وحدات من إجمالي الفاتورة، مُضاف داخل المعاملة | `customers`, `customer_points_log` | داخل المعاملة |
| 11 | COMMIT و `notifyListeners` | السطر 682-692 | أي استثناء قبل هذا يُرجع (ROLLBACK) كامل العملية | — | COMMIT |
| 12 | تنبيه نقص المخزون | `getLowStockIngredients()` + تحديث ملاحظات | 683-691 | **خارج المعاملة** — يلحق تحذيرًا نصيًا بملاحظات الفاتورة؛ فشله تجميلي فقط | `invoices.notes` | **خارج المعاملة (تجميلي)** |

ملاحظات استنتاجية من التتبع: خصم المكونات يتم **تجميعيًا بالمكوّن** (aggregate per ingredient) وليس لكل سطر فاتورة على حدة — أي أن صفين من نفس المنتج في فاتورة واحدة يُحسبان في `delta` واحد (2 × perUnit) ويُسجّلان في صف تدقيق واحد. كذلك الدالة القديمة `DatabaseHelper.deductProductIngredients()` (سطر 829) لا يُستدعى من مسار البيع الحالي؛ المسار ينفّذ خصمه الخاص داخل معاملة `createInvoice` نفسها — وهي ملاحظة إيجابية للذرية (لا معاملة متداخلة).

## مثال السيناريو الإلزامي (المطلوب حرفيًا)

| اليوم | الحدث | الحساب | النتيجة |
|---|---|---|---|
| Day 1 | بيع منتج X (الوصفة: A = 100g) بكمية 1 | `required[A] = 100 * 1` (من الوصفة الحالية) | **خصم المخزون = 100g** |
| Day 2 | تغيير الوصفة: A = 150g | `UPDATE product_ingredients SET quantity = 150` (ConflictAlgorithm.replace — القيمة القديمة تُستبدل نهائيًا دون أي أثر) | الوصفة القديمة **محذوفة نهائيًا** |
| Day 3 | إرجاع فاتورة Day 1 | `restore = current_quantity(A=150) * soldQty(1) = 150g` (سطر 764: قراءة `pi.quantity` الحالية) | **استرجاع = 150g ← خطأ** |

النتيجة الفيزيائية النهائية: المخزون صافي `-100 + 150 = +50g` — **تولّد 50 جرامًا من العدم** لم يُشترَ أصلًا. سجل التدقيق سيُسجّل `quantity_change = +150` مع `action_type='sale_returned'` وكأنه تصحيح صحيح، فلا يمكن اكتشاف الخطأ من السجلات وحدها دون مرجع خارجي.

---

# 3. Inventory Deduction — تحليل خصم المخزون

| السؤال | الإجابة والدليل |
|---|---|
| أين يتم خصم المكوّن؟ | `createInvoice()` سطر 636-651، داخل معاملة الفاتورة نفسها، عبر `UPDATE inventory SET quantity = before - delta` |
| كيف تُحسب الكمية؟ | `delta = Σ (recipe_quantity × sold_qty)` لجميع أسطر الفاتورة التي تحتوي هذا المكوّن (تجميع حسب `ingredient_id`) |
| هل يعتمد الحساب على الوصفة الحالية؟ | **نعم — حاسمًا.** القراءة من `product_ingredients` تتم لحظة البيع (سطر 592) |
| هل تُخزَّن الكمية الفعلية المستخدمة للفاتورة؟ | **غير مباشر فقط** — تُخزَّن في `inventory_audit_log` كـ `quantity_change` مرجوعًا إلى `reference_type='invoice'`/`reference_id`. لا يوجد جدول يربط (فاتورة × منتج × مكوّن × كمية) |
| هل يُخزَّن snapshot للوصفة؟ | **لا — لا يوجد أي عمود أو جدول** |
| هل يُخزَّن snapshot لكمية المكوّن في الوصفة؟ | **لا** |
| هل يُخزَّن snapshot للتكلفة؟ | نعم — `invoice_items.cost_snapshot` لكل منتج (سلوك سليم ماليًا) |
| هل يوجد مرجع تاريخي؟ | جزئي: صف التدقيق يحتفظ بالـ delta الفعلي وقت البيع والتكلفة لحظتها (`cost_price_at_action`) — يمكن استنتاج كمية الخصم الفعلية للفاتورة من مجموع صفوف `action_type='sale'` لنفس `reference_id`، لكن **لا يمكن إثبات** أنها تتطابق مع الوصفة وقت البيع بعد أن تُغيَّر الوصفة |

---

# 4. Inventory Audit — تحليل سجل التدقيق

مخطط `inventory_audit_log` (v6 migration، سطر 578-594):

| الحقل | النوع | ملاحظات |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | |
| action_date | TEXT | ISO8601 محلي |
| action_type | TEXT | `'sale'` / `'sale_returned'` / `'sale_cancelled'` / أنواع أخرى |
| ingredient_id | INTEGER | FK منطقي (بلا قيود FOREIGN KEY صريحة) |
| ingredient_name | TEXT | نسخة نصية من الاسم لحظة التدوين |
| quantity_before / quantity_change / quantity_after | REAL | الحالة الكاملة قبل وبعد الحركة |
| cost_price_at_action | REAL | تكلفة المكوّن لحظتها (معلومة تاريخية قيمة) |
| reference_type / reference_id | TEXT / INTEGER | `'invoice'` + رقم الفاتورة |
| note | TEXT | |

ما ينقص الجدول: **لا عمود للمستخدم المسؤول** (المتابعة عبر `invoice_audit_log` فقط)، ولا قيد UNIQUE يمنع الصفوف المكررة، ولا عمود يحفظ كمية الوصفة لكل سطر (per invoice line).

**هل يمكن استخدام Audit الحالي لإعادة بناء كمية الخصم للفاتورة؟**

**نعم — بحدود.** مجموع `quantity_change` لصفوف `action_type='sale'` حيث `reference_type='invoice'` و`reference_id=N` يعطي بدقة الكمية الفعلية التي خُصمت من كل مكوّن عند البيع، حتى بعد الإرجاع (فصفوف الإرجاع `sale_returned` منفصلة). لكن هذا يعيد بناء **الرقم التاريخي الفعلي** وليس **العلاقة بين الفاتورة والوصفة**: لا يمكن التحقق لاحقًا من أن الكمية المُعادة أثناء الإرجاع مطابقة لما خُصم أصلًا، لأن لا شيء يحفظ `recipe_quantity` وقت البيع.

---

# 5. Recipe History — تتبع تغييرات الوصفات

| السؤال | الإجابة والدليل |
|---|---|
| أين تُخزَّن `product_ingredients`؟ | جدول واحد مسطح (سطر 105-117): `(product_id, ingredient_id, quantity)` مع قيد `UNIQUE (product_id, ingredient_id)` |
| هل يوجد versioning؟ | **لا** |
| هل يوجد effective date؟ | **لا** — يوجد `created_at` فقط (لحظة الإدراج)، ويُستبدل عند التعديل |
| هل يوجد snapshot؟ | **لا** |
| كيف يُعدَّل وصف (Edit quantity)؟ | `recipe_management_screen.dart` سطر 180-199 يستدعي `addProductIngredient()` نفسه، الذي يستخدم `ConflictAlgorithm.replace` على القيد الفريد — **القيمة القديمة تُستبدل نهائيًا دون حفظ** |
| كيف تُضاف/تُحذف مكونات؟ | `addProductIngredient` (insert replace)، `deleteProductIngredient` (حذف الصف) — `deleteAllProductIngredients` موجودة في الطبقة السفلية لكنها غير مستخدمة من الواجهة |
| هل INSERT/UPDATE/DELETE يغيّر التاريخ؟ | **نعم — كل تعديل يُمسح فيه أثر الوصفة السابقة نهائيًا** |
| ماذا يحدث عند حذف مكوّن أو منتج؟ | FK بـ `ON DELETE CASCADE` — حذف المكوّن من المخزون **يمسح الوصفة بصمت** لكل المنتجات المرتبطة، دون إنذار أو تدوين |
| هل تحتفظ الفاتورة بأي ارتباط بالوصفة القديمة؟ | **لا — invoice_items لا يحوي أي مرجع للوصفة (لا ingredient_ids ولا recipe_version)** |
| أثر جانبي موثق | بعد كل تغيير وصفة يُستدعى `updateProductCostFromRecipe()` فيُحدَّث `products.cost` للتكلفة الحالية فورًا (سطر 1540-1547) |

---

# 6. Return Analysis — تحليل `returnInvoice()` (سطر 746-824)

1. **تحديد المنتجات:** `SELECT ... FROM invoice_items WHERE invoice_id = ?` — كل سطر من الفاتورة (بدون ترشيح، بلا دعم جزئي).
2. **تحديد الكمية:** `soldQty = item['quantity']` لكل سطر — الكمية المباعة كاملة (سطر 758).
3. **حساب المكونات:** استعلام مباشر على الوصفة **الحالية**: `SELECT pi.ingredient_id, pi.quantity ... FROM product_ingredients pi INNER JOIN inventory inv ... WHERE pi.product_id = ?` (سطر 759-761).
4. **هل يُستخدم Current recipe؟** **نعم — حاسمًا** (`pi.quantity` قيمة اليوم) (سطر 764: `restore = pi.quantity * soldQty`).
5. **هل تُستخدم بيانات تاريخية؟** **لا** — لا توجد أي قراءة من `invoice_items` لغير `product_id` و`quantity`، ولا أي قراءة من سجل التدقيق.
6. **تعديل المخزون:** `UPDATE inventory SET quantity = before + restore` داخل المعاملة (سطر 767).
7. **التدوين:** صف `inventory_audit_log` بـ `action_type='sale_returned'` و`quantity_change=+restore` و`reference_type='invoice'` و`reference_id` — يوثّق الرقم المسترجَع ولو كان خاطئًا (سطر 768-772).
8. **حالة الفاتورة:** تُحدَّث إلى `status='returned'` و`kitchen_status='done'` داخل المعاملة (سطر 809-815).
9. **الإجماليات:** `returnInvoice` **لا يعدّل** `total_amount` ولا أي حقول مالية على الفاتورة — الأسطر المالية المجمّدة تبقى كما هي، والتقارير (بعد إصلاح Phase 1) تتجاهل الفواتير المُرجعة عبر الحالة.
10. **COGS:** لا يُلمس `cost_snapshot` / `unit_profit` / `total_profit` — سليمة ماليًا.
11. **الأرباح:** لا تعديل مالي مباشر؛ الأثر على الأرباح هو الإقصاء من التقارير بحالة `returned`.
12. **المعاملة:** `returnInvoice` داخل `_db!.transaction<int>` بالكامل — ذري.
13. **النقاط (الولاء):** معكوسة بشكل صحيح بقراءة النقاط الممنوحة فعلًا من `customer_points_log` (وليس إعادة الحساب بقاعدة اليوم) (سطر 781-805).
14. **حماية التكرار:** إذا كانت الحالة `returned` تعيد `0` فورًا (سطر 752)؛ إذا كانت `cancelled` ترمي استثناء (سطر 753).

**RETURN SAFETY VERDICT: `PARTIALLY SAFE`**
- ماليًا: SAFE (snapshots مجمّدة، لا تعديل مالي، نقاط معكوسة بشكل صحيح).
- فيزيائيًا (المخزون): **UNSAFE بعد تغيير الوصفة** — يعيد كميات محسوبة من الوصفة الحالية.
- من حيث الحالة: SAFE (حارس تكرار واضح).
- السبب الجوهري: غياب snapshot الوصفة يجعل الاسترجاع يعتمد على الوصفة الحالية بعد كل تعديل على الوصفة.

---

# 7. Void Analysis — تحليل `voidInvoice()` (سطر 827-906)

بنية مطابقة تمامًا لـ `returnInvoice` مع اختلافين جوهريين:

| الجانب | التفاصيل |
|---|---|
| حارس التكرار | إذا كانت الحالة `cancelled` **أو** `returned` → تعيد `0` (سطر 831) — أوسع من `returnInvoice` |
| استرجاع المخزون | نفس الاستعلام على الوصفة **الحالية** (سطر 837-839) ونفس صيغة `restore = current * soldQty` (سطر 842) |
| نوع التدوين | `action_type='sale_cancelled'` مع نفس الحقول المرجعية (سطر 847-851) |
| تحديث الحالة | `status='cancelled'` مع شرط `WHERE id=? AND status != 'cancelled'` — تحديث مشروط ذكي، وسجل `invoice_audit_log('cancelled')` يُدوَّن فقط إذا نجح التحديث فعلاً (`updated > 0`) (سطر 888-901) |
| المعاملة | `_db!.transaction<int>` بالكامل — ذري |
| التعريفات | `deleteInvoice(id) => voidInvoice(id)` (سطر 908) |
| COGS/الأرباح | لا يُلمس — نفس السلوك المالي السليم |

**VOID SAFETY VERDICT: `PARTIALLY SAFE`**
- نفس حكم الإرجاع: ماليًا SAFE، فيزيائيًا غير آمن بعد تغيير الوصفة، وحمايته من التكرار **أفضل** من `returnInvoice` (يعالج `returned` و`cancelled` بصمت بدل رمي استثناء).

---

# 8. Duplicate Reversal Analysis — تحليل التكرار المزدوج

| السيناريو | السلوك الفعلي من الكود | النتيجة |
|---|---|---|
| Return مرتين | الأولى تعيد المخزون وتحوّل الحالة إلى `returned`؛ الثانية تصطدم بسطر 752 وتعيد `0` فورًا بلا أي تغيير | SAFE — لا استرجاع مزدوج |
| Void مرتين | الأولى تُحوّل إلى `cancelled`؛ الثانية تصطدم بسطر 831 وتعيد `0` (الحالة في `cancelled` أو `returned` كلاهما حارس) | SAFE |
| Return ثم Void | `voidInvoice` على فاتورة `returned` يعيد `0` فورًا (سطر 831) بلا استثناء وبلا تغيير | SAFE — الحالة النهائية `returned` بلا أي استرجاع إضافي |
| Void ثم Return | `returnInvoice` على فاتورة `cancelled` **يرمي استثناء** "الفاتورة ملغاة بالفعل" (سطر 753)؛ لم يحدث استرجاع | SAFE من ناحية المخزون — لكن السلوك API غير متسق (void يتسامح، return يرمي استثناء) |
| Return بعد تغيير الحالة إلى cancelled من خارج | نفس الاستثناء — السلوك متسق | SAFE |
| Void بعد Return | معالج أعلاه (يعيد 0) | SAFE |

**الخلاصة:** لا يوجد أي مسار يؤدي إلى استرجاع مخزون مزدوج أو تدوين مزدوج أو عكس COGS مزدوج — الحراس على مستوى الحالة (status guards) تعمل بشكل صحيح في جميع التركيبات الست. الفارق الوحيد هو **عدم اتساق واجهة API** (حارس صامت في void مقابل استثناء في return)، وهو سلوك لا يخل بالأمان لكنه يستحق توحيدًا في مرحلة لاحقة. يُلاحظ كذلك أن الحمايات جميعها تسلسلية: لا قفل صف (row-level lock) على `invoices`؛ التوازي بين مديرين نادر في تطبيق محلي، لكن لا يوجد ضمان ذري كامل ضد race بين استدعائين متزامنين على نفس الفاتورة.

---

# 9. Partial Return — الدعم الجزئي

النظام **لا يدعم** أي شكل من أشكال الإرجاع الجزئي. الكود ي_LOOP على **كل** `invoice_items` للفاتورة دون أي معامل ترشيح (سطر 754-773 في return و832-852 في void):

| النوع | مدعوم؟ | الدليل |
|---|---|---|
| Full invoice return | نعم | السلوك الوحيد المطبق |
| Partial item return (صنف واحد من عدة أصناف) | **لا** | لا معامل `items[]` ولا ترشيح |
| Partial quantity return (جزء من كمية صنف) | **لا** | الكمية المقروءة هي `item['quantity']` كاملة |

التوثيق فقط كما طُلب — لا اقتراحات تنفيذ.

---

# 10. Transaction Atomicity — الذرية

| العملية | البنية | ما يحدث عند الفشل في المنتصف |
|---|---|---|
| Sale (`createInvoice`) | `BEGIN` → (insert invoices → insert invoice_items ×n → update inventory ×m → insert inventory_audit_log ×m → insert invoice_audit_log → update customers) → `COMMIT` | SQLite transaction واحدة؛ أي استثناء قبل COMMIT يؤدي إلى ROLLBACK كامل — لا فاتورة جزئية ولا خصم جزئي. الاستثناء الوحيد خارج المعاملة هو تحديث ملاحظات نقص المخزون (سطر 683-691) وهو تجميلي |
| Return (`returnInvoice`) | `BEGIN` → (read invoice + items → update inventory ×m → insert audit ×m → update customers → update invoices status → insert invoice_audit_log) → `COMMIT` | ذري بالكامل؛ الفشل في أي خطوة يعيد كل شيء بما فيه تغيير الحالة |
| Void (`voidInvoice`) | `BEGIN` → (مطابق لـ Return + تحديث حالة مشروط بـ `AND status != 'cancelled'`) → `COMMIT` | ذري بالكامل |

الترتيب داخل معاملة البيع مهم: التحقق من توفر المخزون يتم **قبل** المعاملة (خطوة 3)، وداخل المعاملة يُعاد قراءة الكمية لحظيًا (سطر 638) مع حارس سالب. لا يوجد معاملة داخل معاملة (nested) في المسار الحالي — النمط السابق الموثّق في تعليق `deductProductIngredients` (تحذير من concur transactions) لم يعد مطبقًا؛ مسار البيع يخصم مباشرة داخل معاملة الفاتورة.

---

# 11. Historical COGS — تحليل التكلفة التاريخية

| السؤال | الإجابة والدليل |
|---|---|
| COGS على `invoice_items` | **Snapshot مجمّد** — `cost_snapshot` يُحسب مرة واحدة عند البيع (`calculateProductCost` سطر 608) ويُخزَّن دائمًا (سطر 632) |
| عند تغيير الوصفة | لا أثر — الـ snapshot محفوظ؛ لكن `products.cost` (الحقل الحالي) يُحدَّث فورًا بـ `updateProductCostFromRecipe` (للعروض الحالية فقط) |
| عند تغيير تكلفة المكوّن (شراء جديد) | `invoice_items` لا تتأثر (snapshot). لكن `recalculateProductCostsForIngredient()` (سطر 1000-1031) يُحدِّث `products.cost` لجميع المنتجات التي تستخدم المكوّن — التكلفة **الحالية** فقط |
| عند Return | لا لمس للـ snapshot المالي؛ التقارير تستبعد الفواتير المُرجعة بالحالة |
| عند Void | لا لمس للـ snapshot المالي |

**خلاصة COGS: SAFE.** النظام يملك فصلًا سليمًا بين التكلفة التاريخية (مجمّدة على السطر) والتكلفة الحالية (على جدول `products`/`inventory`)، وهذا الفصل هو الذي مكّن إصلاحات Phase 1/1.1 من العمل بشكل صحيح. المشكلة الوحيدة المتبقية هي **كميات المخزون** (المرتبطة بالوصفة) وليس **التكاليف**.

---

# 12. Scenario Matrix — مصفوفة السيناريوهات

| # | السيناريو | السلوك الفعلي | تاريخيًا آمن؟ | الدليل |
|---|---|---|---|---|
| 1 | بيع ثم إرجاع بدون تغيير وصفة | استرجاع مطابق للخصم (نفس `pi.quantity`) | **نعم** | سطر 764 = سطر 595 × نفس الكمية المباعة |
| 2 | بيع ثم تغيير وصفة ثم إرجاع | استرجاع بالوصفة الجديدة (مثال الإلزامي: خصم 100g → استرجاع 150g = +50g زائفة) | **لا** | سطر 759-764 يقرأ الوصفة الحالية |
| 3 | بيع ثم تغيير تكلفة مكوّن ثم إرجاع | استرجاع الكمية صحيح؛ `cost_price_at_action` في صف الاسترجاع يسجل التكلفة الجديدة (معلومة لا أثر ماليًا) — COGS المالي غير متأثر | **نعم** (فيزيائيًا) / ماليًا سليم دائمًا | سطر 764 (كمية) + سطر 771 (cost الحالي) + snapshots مجمدة |
| 4 | بيع ثم إلغاء (Void) | استرجاع كامل مطابق للخصم؛ حالة `cancelled`؛ تدوين كامل | **نعم** (بدون تغيير وصفة) | سطر 837-851 + 888-894 |
| 5 | بيع ثم تغيير وصفة ثم إلغاء | نفس خلل السيناريو 2 — استرجاع بالوصفة الجديدة | **لا** | نفس سطر 837-842 |
| 6 | إرجاع مرتين | الثانية تعود `0` فورًا (status guard) | **نعم** | سطر 752 |
| 7 | إلغاء مرتين | الثانية تعود `0` (status guard) | **نعم** | سطر 831 |
| 8 | إرجاع ثم إلغاء | `void` على `returned` يعود `0` بصمت؛ الحالة تبقى `returned` | **نعم** | سطر 831 |
| 9 | إلغاء ثم إرجاع | `return` على `cancelled` يرمي استثناء؛ لا مخزون يتغير | **نعم** | سطر 753 (استثناء — انعدام اتساق API فقط) |
| 10 | عدة منتجات متطابقة في فاتورة واحدة | الخصم يُجمَّع صحيحًا (`perUnit * qty` لكل سطر ضمن نفس `delta`) والإرجاع يُعيد المجموع كاملًا لكل سطر | **نعم** | سطر 596 + 636-651؛ الإرجاع ي_LOOP على الصفوف فرديًا فيُعاد المجموع نفسه |
| 11 | فواتير متعددة لنفس المنتج | كل فاتورة تخصم/تستعيد مستقلًا بكمياتها — لا تداخل | **نعم** | المعاملات منفصلة و`reference_id` يفصل التدوين |
| 12 | إرجاع جزئي (كمية/صنف) | غير مدعوم إطلاقًا | N/A | سطر 754: حلقة على كل الصفوف بلا ترشيح |

---

# 13. Risk Classification — تصنيف المخاطر

| الدرجة | الرمز | الخطر | الوصف | الدليل |
|---|---|---|---|---|
| **P0 — حرج** | R1 | **فساد المخزون الفيزيائي بعد تغيير الوصفة** | كل إرجاع/إلغاء بعد تعديل الوصفة يعيد كمية خاطئة؛ يتركم الخطأ في كل عملية (مخزون متضخم أو منقوص دائمًا) مع تدوين يبدو سليمًا | §2، §6، §7 |
| **P1 — مرتفع** | R2 | **غياب كامل لأي سجل تاريخي للوصفات** | لا يمكن أصلًا إعادة التحقق من صحة أي استرجاع أو خصم تاريخي، ولا حساب COGS فيزيائي بأثر رجعي، ولا تلبية أي مطلب تدقيق | §5 |
| **P1 — مرتفع** | R3 | **CASCADE صامت على حذف المكوّن/المنتج** | حذف مكوّن من `inventory` يمسح وصفاته لجميع المنتجات دون إنذار — فقدان مرجع الخصم/الاسترجاع مستقبلًا | UNIQUE + ON DELETE CASCADE (سطر 113-115) |
| **P2 — متوسط** | R4 | **عدم اتساق واجهة الـ API بين void وreturn** | void يتسامح مع الحالات النهائية (يعيد 0) بينما return يرمي استثناء على `cancelled` — اختلاف سلوك يحتاج توحيدًا | سطر 753 مقابل 831 |
| **P2 — متوسط** | R5 | **بلا حماية على مستوى الصف ضد الاستدعاءات المتزامنة** | لا قفل صف للفاتورة؛ استدعاءان متزامنان من مديرَين على نفس الفاتورة قد يتخطيان الحارس معًا (احتمال عملي منخفض في تطبيق محلي) | غياب `FOR UPDATE`/قفل |
| **P3 — منخفض** | R6 | **inventory_audit_log بلا عمود مستخدم** | متابعة المسؤولية تتطلب الربط اليدوي بـ `invoice_audit_log` | مخطط v6 (سطر 580-593) |
| **P3 — منخفض** | R7 | **تحديث ملاحظات نقص المخزون خارج المعاملة** | تجميلي؛ فشل غير محسوس وظيفيًا | سطر 683-691 |
| **P3 — منخفض** | R8 | **`deductProductIngredients()` دالة مهملة** | لا تُستدعى؛ وجودها مربك وصيانتها عبء زائد (خطر مستقبلي إذا استُخدمت) | سطر 829-878 |

---

# 14. Solution Options — خيارات الحل (توصيات فقط، بدون تنفيذ)

> ملزم من متطلبات المرحلة: **لا تنفيذ لأي حل** — ما يلي خيارات تحليلية للمرحلة التالية.

## Option A — Recipe Versioning (تخزين نسخة الوصفة لكل سطر فاتورة)

**البنية:** إضافة جدول `product_ingredients_snapshot` (أو أعمدة `recipe_snapshot JSON` على `invoice_items`) تُملأ وقت البيع: لكل سطر فاتورة، نسخة كاملة من الوصفة السارية (`ingredient_id` + `quantity` لكل مكوّن). يتم تعديل `returnInvoice`/`voidInvoice` ليقرأ الـ snapshot بدل `product_ingredients` الحالية.

**الأثر على قاعدة البيانات:** جدول جديد أو أعمدة JSON؛ مؤشر على `invoice_id`.

**الأثر على Migration:** Migration v16 بسيطة (CREATE TABLE فقط) — بلا أي تحويل للبيانات التاريخية.

**السلامة التاريخية:** كاملة للفواتير الجديدة من لحظة التطبيق؛ التاريخية تبقى محسوبة بالوصفة الحالية (سلوك اليوم)، لكن توثيقها لا يتدهور أكثر.

**التعقيد:** متوسط.

**المخاطر:** زيادة حجم التخزين لكل سطر فاتورة (محدودة عمليًا: بضع عشرات بايت).

| الإيجابيات | السلبيات |
|---|---|
| حل جذري للسبب: المرجع التاريخي موجود دائمًا | لا يصلح الفواتير التاريخية بأثر رجعي |
| استرجاع دقيق 100% مهما تغيرت الوصفة أو تُحذف المكونات | يمسّ `createInvoice` وreturn/void (خارج نطاق مقفل سابقًا) |
| يمكّن لاحقًا من تقارير COGS فيزيائية بأثر رجعي | — |

## Option B — Snapshot في Audit Log (حفظ الكميات في سجل التدقيق)

**البنية:** إضافة عمود `recipe_quantity REAL` (أو JSON) إلى صف `inventory_audit_log` في حركات `sale`، بحيث يحمل كل صف كمية الوصفة المستخدمة لكل مكوّن. `returnInvoice`/`voidInvoice` يقرآن مجموع الكميات التاريخية من صفوف `sale` لنفس `reference_id` بدل الوصفة الحالية.

**الأثر على قاعدة البيانات:** عمود واحد على جدول موجود — لا جداول جديدة.

**الأثر على Migration:** v16 بـ `ALTER TABLE ... ADD COLUMN`؛ البيانات القديمة NULL تُعالَج بـ fallback للسلوك الحالي.

**السلامة التاريخية:** نفس نطاق Option A (من لحظة التطبيق فصاعدًا).

**التعقيد:** منخفض-متوسط.

**المخاطر:** فصل بيانات الاسترجاع عن الفاتورة نفسها (query أعقد)؛ دمج عدة منتجات مشتركة في مكوّن واحد في صف واحد يجعل الاسترجاع الجزئي مستحيلًا من الـ snapshot وحده.

| الإيجابيات | السلبيات |
|---|---|
| أقل تغيير ممكن على المسارات | لا يدعم مستقبلًا partial return |
| يعزز قيمة audit log كأصل تدقيق | القراءة من سجلات متعددة أعقد من snapshot على السطر |
| يتوافق مع نمط التدقيق الموجود (reference_type/reference_id) | — |

## Option C — منع تغيير الوصفة على منتجات بمبيعات نشطة + توثيق التغييرات

**البنية:** إضافة جدول `recipe_change_log` (ماذا تغيّر، متى، من) + فحص وقت تعديل الوصفة: إذا كان للمنتج فواتير غير مُرجعة خلال نافذة زمنية (مثلاً آخر 30 يومًا)، اطلب تأكيد المدير وصِل الاسترجاع ضمن النافذة بالوصفة القديمة المسجلة في اللوج. خارج النافذة يُسمح بالاسترجاع بالوصفة الحالية (مقبول إحصائيًا لأن الفواتير التاريخية المسترجعة نادرة).

**الأثر على قاعدة البيانات:** جدول لوج تغييرات.

**الأثر على Migration:** v16 CREATE TABLE.

**السلامة التاريخية:** يقيّد الخطر بدل أن يقضي عليه — الفواتير الحديثة خارج النافذة تبقى معرضة.

**التعقيد:** منخفض.

**المخاطر:** حل جزئي؛ يضيف قيودًا عملية على عمل الطاقم (تعديل الوصفات يصبح مقيدًا).

| الإيجابيات | السلبيات |
|---|---|
| أسهل تطبيق وأقل تكلفة هندسية | لا يحل المشكلة جذريًا — يقلل التكرار فقط |
| يضيف الشفافية (من غيّر الوصفة ومتى) المفقودة حالياً | منطق النافذة الزمنية اجتهادي قابل للنقاش |

## RECOMMENDED OPTION: **A (Recipe Versioning على invoice_items)**

التوصية: **Option A**، ويفضَّل بتنفيذ هجين مع أقوى نقاط B — حفظ snapshot الوصفة كاملًا في عمود JSON على `invoice_items` (تفادي جدول إضافي مع روابط مفتوحة)، مع قراءة الإرجاع/الإلغاء من الـ snapshot وfallback للوصفة الحالية للبيانات القديمة (NULL). هذا الخيار يقضي على السبب الجذري R1/R2 نهائيًا، ويحافظ على السلوك التاريخي دون تدهور، ويمكّن مستقبلًا من partial return وCOGS فيزيائي بأثر رجعي. يُستكمل بجدول `recipe_change_log` (من Option C) كتوثيق إلزامي لأي تعديل وصفة، لأن غياب أثر التغيير نفسه مشكلة تدقيق مستقلة (R2).

**مؤجل للمرحلة القادمة** (كما طُلب): التنفيذ الفعلي — لا ننفذ شيءًا في هذه المرحلة.

---

# 15. Files Inspected — الملفات المفحوصة

| الملف | الغرض |
|---|---|
| `lib/providers/app_provider.dart` | `createInvoice` (535-693)، `returnInvoice` (746-824)، `voidInvoice` (827-906)، `deleteInvoice` (908)، `addProductIngredient`/`deleteProductIngredient` (1541-1560)، `updateProductCostFromRecipe` |
| `lib/core/database/database_helper.dart` | المخططات كاملة (`products`، `inventory`، `product_ingredients`، `invoices`، `invoice_items`، `inventory_audit_log`، `invoice_audit_log`)، الم.Migrations v6-v14، CRUD الوصفات، `recalculateProductCostsForIngredient`، `deductProductIngredients`، `logInventoryAudit` |
| `lib/screens/products/recipe_management_screen.dart` | سلوك الواجهة لإضافة/تعديل/حذف مكونات الوصفة (سطر 120-250) |
| `lib/screens/invoices/invoices_screen.dart` | نقاط استدعاء return/void من الواجهة (سطر 90-119، 292) |
| `lib/core/utils/financial_calculator.dart` | استُخدم كمرجع فقط للتحقق من أن التقارير لا تلمس الكميات الفيزيائية (لا تعديل) |

---

# 16. Database Tables Inspected — الجداول المفحوصة

| الجدول | الصلة بالتدقيق |
|---|---|
| `invoices` | حالة الفاتورة (completed/cancelled/returned) — محور كل حراس التكرار |
| `invoice_items` | `cost_snapshot` (snapshot مالي سليم) — **بلا** أي مرجع للوصفة |
| `products` | `cost` حالي (يُعاد حسابه عند كل تغيير وصفة/تكلفة) |
| `product_ingredients` | الوصفة **الوحيدة والحالية** — بلا versioning أو snapshot؛ UNIQUE + CASCADE |
| `inventory` | الكميات الحالية وتكلفة الشراء الحالية |
| `inventory_audit_log` | سجل الحركات الفيزيائية (sale/sale_returned/sale_cancelled) مع المراجع — المصدر الوحيد لإعادة بناء الخصم الفعلي |
| `invoice_audit_log` | سجل أحداث الفاتورة مع المستخدم المسؤول (created/returned/cancelled) |
| `customer_points_log` | عكس النقاط الصحيح في الإرجاع/الإلغاء (مرجع للتحقق) |

---

# 17. Out-of-Scope Findings — ملاحظات خارج نطاق المرحلة

هذه ملاحظات قُدِّمت للتوثيق فقط (كما عُمل في Phase 1.1 مع finding الخصم عند الإدخال) ولا تتطلب عملًا الآن:

1. **قابلية الإرجاع المالي:** `returnInvoice` لا يعكس أي حقول مالية على الفاتورة (الإجماليات ثابتة) — التقارير تعتمد على الحالة؛ هذا نمط سليم لكن يستحق مراجعة متعمقة في مرحلة لاحقة للتأكد من أن كل التقارير تستبعد `returned/cancelled` اتساقًا.
2. **`updateProductCostFromRecipe` بعد حذف/إضافة مكون** يغيّر `products.cost` الحالي — أي عرض سعر "التكلفة" لحظة البيع يعتمد على لحظة الاستعلام لا على لحظة البيع؛ غير مؤثر ماليًا (لأن snapshot محفوظ) لكن مؤثر تشغيليًا على قرارات التسعير.
3. **غياب أي audit على جدول `product_ingredients`** — تغيير الوصفة نفسه غير مرئي في أي سجل؛ إذا كان شخص غيّر الوصفة يومًا ما، لا يمكن إثبات ذلك أو استرجاعه.
4. **دالة `deductProductIngredients` المهملة** في `database_helper.dart` تستحق حذفًا أو توثيقًا `@deprecated` في مرحلة تنظيف لاحقة لتفادي استخدامها بالخطأ (ستعطي سلوك خصم مختلفًا عن مسار البيع الحالي).

---

# PHASE 2.0 STATUS

**PASS — Audit Complete**

| المؤشر | القيمة |
|---|---|
| Files Modified | **0** |
| Database Modified | **NO** |
| Schema Modified | **NO** |
| Migration Modified | **NO** |

تم هذا التدقيق بالكامل بقراءة وتحليل الكود المصدري والمخططات فقط. لم يُعدَّل أي ملف في المستودع عدا هذا التقرير الوثائقي.
